grafana:
  # Włączamy trwały dysk PVC o pojemności 2 Gigabajtów
  persistence:
    enabled: true
    type: pvc
    size: 2Gi

  grafana.ini:
    auth.anonymous:
      enabled: true
      org_name: Main Org.
      org_role: Viewer
    auth:
      disable_login_form: false